import PageClient from './page-client';

export const metadata = {
  title: 'Perfil - Panel Entrenador V2',
  description: 'Perfil profesional del entrenador',
};

export default function TrainerV2ProfilePage() {
  return <PageClient />;
}
