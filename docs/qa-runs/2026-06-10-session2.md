# QA Run — 2026-06-10 — Sesión 2 (pendientes + cierre diagnóstico)

## Cabecera

| Campo | Valor |
|-------|-------|
| **Fecha** | 2026-06-10 |
| **Tester** | Agente (API + inspección estática UI) |
| **Entorno** | Docker + Ollama |
| **Script nuevo** | `backend/scripts/run_qa_session2.py` |
| **Corrida previa** | [`2026-06-10.md`](2026-06-10.md) |

## Resumen global

| Métrica | Session 2 | Smoke (post-restart) | Full (post-restart) | CI F |
|---------|-----------|----------------------|---------------------|------|
| ✅ Pass | **8** | **7** | **36** | **118+25+build** |
| ⚠️ Conocido | **8** | 0 | **12** | — |
| ⏭️ Skip | **4** | **1** (S4) | **7** | — |
| ❌ Fail | **0** | **0** | **5*** | **0** |

\* **Full A2, A5, A6, B10, B13** fallaron por **429 rate limit** al encadenar scripts sin restart entre session2→smoke→full en la misma ventana. **A6 revalidado ✅** en session2 aislado. No son bugs de producto.

---

## Casos pendientes — Session 2

| ID | Resultado | Notas |
|----|-----------|-------|
| **A6** | ✅ | Login inactivo → **401** (script aislado, sin prefetch) |
| **A10** | ⏭️ | `ProtectedRoute` + `role-routes.ts`; redirect en browser manual |
| **C5** | ✅ | API: unassign → `isActive=false` → reassign → activo |
| **C6** | ✅ | Plan semanal: PUT doble con payload completo; lunes+martes persisten |
| **C8** | ⚠️ | `progress-overview.tsx` no consume API métricas (12.1) |
| **C10** | ⚠️ | Botón Editar sin `onClick` (11.2) |
| **C12** | ⏭️ | Navbar por rol OK en código; no probado en browser |
| **D2** | ⚠️ | KPI asignaciones sin enlace a `/admin/assignments` (10.7) |
| **D5** | ⚠️ | Modal/listado sin métricas embebidas (12.2) |
| **D7** | ✅ | Invite + **resend** 200 (API); UI modal manual |
| **D8** | ⏭️ | API DELETE probada en corrida previa; skip si trainer ya inactivo |
| **D9** | ⚠️ | PATCH reactivar → **405** (11.5) |
| **D11** | ✅ | Rutina admin **visible** en `GET /routines/` sin `trainerId` |
| **E3** | ⚠️ | Sin interceptor 401 global en `http-client.ts` (13.1) |
| **E4** | ✅ | Atleta1 → rutina exclusiva de athlete2 → **403** |
| **E8** | ⚠️ | API change-password OK; perfil sin UI (11.1) |
| **B8** | ✅ | `toast.error` en fallo diario (`use-nutrition.ts`) — FIX-06 |
| **B14** | ⚠️ | `/settings` → 404; enlace roto (8.5) |
| **FIX-04** | ✅ | `resolveAthleteId` + Vitest |
| **FIX-05** | ⏭️ | Helper eliminado; IDOR cubierto en backend (C11/E5/E6) |

---

## Revalidación Sprint 1 (FIX-01…07)

| FIX | Estado tras session 2 | Evidencia |
|-----|----------------------|-----------|
| FIX-01 | ✅ **Corregido** | `weekly-plan-editor.tsx` precarga GET; C6 API ✅ |
| FIX-02 | ✅ **Corregido** | `isActive` en UI + API unassign/reassign; C5 ✅ |
| FIX-03 | ✅ **No reproducido** | D11: admin list incluye rutinas `admin_id` |
| FIX-04 | ✅ **Corregido** | Vitest + código |
| FIX-05 | ⏭️ **Obsoleto** | Función removida; backend autoriza |
| FIX-06 | ✅ **Corregido** | Toast en `persistDiaryChange` |
| FIX-07 | ⚠️ **Pendiente** | D2 confirmado |

---

## Smoke + Full + CI (misma sesión)

| ID | Resultado | Notas |
|----|-----------|-------|
| S1–S3, G1–G4 | ✅ | Tras restart backend |
| S4 | ⏭️ | DevTools manual |
| E7 | ✅ | `--e7-only` → 429 |
| F1–F5 | ✅ | pytest 118, eslint, tsc, vitest 25, build |

---

## Bloqueadores release (post-diagnóstico)

**Críticos / altos (Sprint 2–4):** C10, E8, E3, D9, C8, D5, B14, FIX-17…

**Sprint 1:** solo **FIX-07** (KPI admin) queda pendiente de los originales.

---

## Comandos

```text
docker compose -p fittrack restart fittrack-backend
python backend/scripts/run_qa_session2.py
docker compose -p fittrack restart fittrack-backend
python backend/scripts/run_qa_smoke.py
python backend/scripts/run_qa_full.py
docker compose -p fittrack restart fittrack-backend
python backend/scripts/run_qa_full.py --e7-only
cd backend && python -m pytest -q
pnpm lint && pnpm typecheck && pnpm test && pnpm build
```

## Credenciales

`password123` · `admin@fittrack.qa` · `trainer1@fittrack.qa` · `athlete1@fittrack.qa` · `athlete31@fittrack.qa`

## Siguiente paso

**Diagnóstico cerrado** → implementar fixes según [`QA_DIAGNOSTICO_Y_PLAN.md`](../QA_DIAGNOSTICO_Y_PLAN.md), empezando por **Sprint 2** (UI cableada) + **FIX-07**.

---

## Re-QA post Sprint 1–2 (2026-06-10, tarde)

Tras implementar **FIX-07…FIX-13**, rebuild del backend y fix de `wait_for_api()` en el script.

### Comandos

```text
docker compose exec fittrack-backend python scripts/seed_qa_dataset.py
docker compose -p fittrack build fittrack-backend
docker compose -p fittrack up -d fittrack-backend
python backend/scripts/run_qa_session2.py
docker compose -p fittrack restart fittrack-backend
python backend/scripts/run_qa_smoke.py
cd backend && python -m pytest -q tests/test_auth.py tests/test_trainer_invitations.py tests/test_authz.py tests/test_users_me.py
```

### Resultados

| Script | Pass | Fail | Known | Skip |
|--------|------|------|-------|------|
| `run_qa_session2.py` | **12** | **0** | 4 | 4 |
| `run_qa_smoke.py` (post-restart) | **7** | **0** | 0 | 1 |
| pytest (auth + users_me + invitations + authz) | **51** | 0 | — | — |

### Casos Sprint 1–2 revalidados

| ID | Antes | Después |
|----|-------|---------|
| **D2** | ⚠️ KPI incoherente | ✅ FIX-07 |
| **D9** | ⚠️ PATCH 405 | ✅ PATCH reactivar **200** (FIX-12) |
| **C10** | ⚠️ Editar sin acción | ✅ inspección estática (FIX-09) |
| **E8** | ⚠️ sin UI contraseña | ✅ perfil + modal (FIX-08) |
| **D8** | ⏭️ | ⏭️ trainer ya inactivo (corrida previa) |

**Sprint 1 y Sprint 2 cerrados.** Pendiente: Sprints 4–5 (FIX-16…FIX-24).

---

## Re-QA post Sprint 3 (2026-06-10)

| FIX | Evidencia |
|-----|-----------|
| FIX-14 (C8) | `progress-overview.tsx` + `useAthleteMetrics` + gráficos |
| FIX-15 (D5) | `latestMetric` en listados + modal con hook |
| FIX-20 | Pestaña Diario en `NutritionCoachEditor` |
| FIX-21 | `GET /users/athletes/:id/body-profile` |
| FIX-22 | Migración `006_trainer_max_athletes` + UI assignments |

| Validación | Resultado |
|------------|-----------|
| `pytest tests/test_sprint3_visibility.py` | 7 passed |
| `pnpm typecheck` + `lint` + `vitest` | ✅ |
| `run_qa_session2.py` C8/D5 | pass (inspección estática) |

Rebuild backend Docker tras migración antes de QA API en contenedor.
